               // Intentional Error:
let list=["Isalamabad", "Paris", "Switzerland", "Apple", "Mango", "Football"];
              
for (let i = 0; i < list.length; i++) {
    const element = list[i];
    
}
        //    index error
console.log("index error:", list[6]);
       // correct the error:
console.log("correct the error:", list[2]);